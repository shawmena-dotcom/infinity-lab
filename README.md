# infinity-lab

Public build-and-test runner for Project Infinity. Product sources are stored encrypted in `bundles/` and
are only decrypted inside the CI job; sellable packages are uploaded encrypted. Only logs and marketing
images are public.
